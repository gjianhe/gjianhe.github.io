
# 栈顶和栈底


栈（Stack）是一种线性数据结构，它遵循后进先出（LIFO, Last In First Out）的原则。这意味着最后被添加到栈中的元素将是第一个被移除的元素。

在栈中，“栈顶”和“栈底”是指栈的两个逻辑端：

1. **栈顶（Top of Stack）**：这是栈中最后添加的元素所在的位置。所有的插入（压栈，Push）和删除（弹栈，Pop）操作通常都在栈顶进行。栈顶元素是当前可以访问的唯一元素。
    
2. **栈底（Bottom of Stack）**：这是栈中最先添加的元素所在的位置。在正常的栈操作中，栈底通常是固定的，不会在此位置执行插入或删除操作。
